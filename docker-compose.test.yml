services:
  overseer-test:
    build:
      context: .
      dockerfile: Dockerfile.test
    container_name: overseer-test
    privileged: true
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    tmpfs:
      - /run
      - /run/lock
    restart: always
